import type React from "react"
import "./globals.css"
import { Inter } from "next/font/google"
import { ThemeProvider } from "@/components/theme-provider"

const inter = Inter({ subsets: ["latin"] })

export const metadata = {
  title: "WebTest - Cybersecurity Services | Secure Development & Pentesting",
  description:
    "Professional cybersecurity services including secure web development with OWASP standards and expert penetration testing for web applications and Active Directory environments.",
  keywords: "cybersecurity, penetration testing, secure development, OWASP, web security, Active Directory, pentest",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className}>
        <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
          {children}
        </ThemeProvider>
      </body>
    </html>
  )
}
